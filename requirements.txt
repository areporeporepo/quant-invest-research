fastapi>=0.110
uvicorn[standard]>=0.29
requests>=2.31
python-dotenv>=1.0
pytest>=8.0
# Full-resolution satellite crops (keyless windowed COG reads).
# The app still runs without these — it falls back to whole-tile previews.
rasterio>=1.3
numpy>=1.26
pillow>=10.0
